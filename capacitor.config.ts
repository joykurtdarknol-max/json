import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.cinayetmasasi.dedektif',
  appName: 'Cinayet Masası',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
